---
import BaseLayout from '../layouts/BaseLayout.astro';
import services from '../data/services.json';

const title = "Servicios | SIXCORES | IT Solutions";
const description = "Descubre los servicios que ofrecemos en SIXCORES: diseño web, soporte técnico y reparación de ordenadores para hogares y negocios.";
---

<BaseLayout {title} {description}>
  <section class="py-16 bg-white dark:bg-gray-950">
    <div class="max-w-7xl mx-auto px-4">
      <h1 class="text-4xl font-bold text-gray-900 dark:text-white mb-6 text-center">
        Nuestros Servicios
      </h1>
      <p class="text-gray-600 dark:text-gray-300 max-w-2xl mx-auto text-center mb-12">
        Soluciones informáticas personalizadas y profesionales para cubrir todas tus necesidades tecnológicas. Aquí puedes ver en detalle cómo podemos ayudarte.
      </p>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        {services.map(service => (
          <div class="bg-gray-50 dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-xl p-6 shadow hover:shadow-lg transition">
            <div class="text-5xl mb-4">{service.icon}</div>
            <h2 class="text-2xl font-semibold text-gray-800 dark:text-white mb-2">
              {service.title}
            </h2>
            <p class="text-sm text-gray-600 dark:text-gray-300">{service.description}</p>
          </div>
        ))}
      </div>
    </div>
  </section>
</BaseLayout>
